source "https://rubygems.org"

# Use github-pages gem if not building locally, but you will have to remove jekyll-scholar
# gem "github-pages", group: :jekyll_plugins
gem 'jekyll', "~>4.0.0", group: :jekyll_plugins

gem 'jekyll-target-blank', "~>2.0.0"
gem 'kramdown-math-katex', "~>1.0.1"

group :jekyll_plugins do
    gem 'jekyll-scholar'
    gem 'jekyll-katex'
end
